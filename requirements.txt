# System prerequisite: FFmpeg + FFprobe (for video compression)
#   Linux: install via your package manager (apt/dnf/yum/pacman/zypper)
#   Windows: downloaded automatically by main.py on first run
PySide6>=6.5.0
Pillow>=10.0.0
pillow-heif>=0.13.0
psutil>=5.9.0
# Optional: NVIDIA GPU monitoring (not required for basic usage)
# nvidia-ml-py3>=7.352.0
